# 🎯 量化主线共振狙击系统 - 架构说明

## 📐 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    股票分析系统 GUI (gui2.py)                      │
│                                                                 │
│  ┌──────────┬──────────┬──────────┬────────────────────────┐   │
│  │📈单只分析 │📊批量分析 │🌟推荐列表 │ 🎯量化主线狙击 (NEW!)   │   │
│  └──────────┴──────────┴──────────┴────────────────────────┘   │
│                                    │                            │
│                                    ▼                            │
│         ┌──────────────────────────────────────────┐           │
│         │   quant_mainline_tab.py (界面层)         │           │
│         │                                          │           │
│         │  ┌────────────────────────────────────┐ │           │
│         │  │  顶部控制栏                        │ │           │
│         │  │  [🔍盘前扫描] [📡监控] [📥导出]    │ │           │
│         │  └────────────────────────────────────┘ │           │
│         │                                          │           │
│         │  ┌────────────────────────────────────┐ │           │
│         │  │  🎯 主线行业排名表                 │ │           │
│         │  │  显示行业强度、状态、持续天数      │ │           │
│         │  └────────────────────────────────────┘ │           │
│         │                                          │           │
│         │  ┌────────────────────────────────────┐ │           │
│         │  │  👑 龙头股池表                     │ │           │
│         │  │  显示综合龙头、资金龙头            │ │           │
│         │  └────────────────────────────────────┘ │           │
│         │                                          │           │
│         │  ┌────────────────────────────────────┐ │           │
│         │  │  🎣 超跌候选表                     │ │           │
│         │  │  显示超跌系数、技术背离            │ │           │
│         │  └────────────────────────────────────┘ │           │
│         └──────────────────────────────────────────┘           │
│                          │                                      │
└──────────────────────────┼──────────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────┐
        │   QuantMainlineThread (分析线程)      │
        │   - 避免阻塞GUI                       │
        │   - 实时进度反馈                      │
        │   - 错误处理                          │
        └──────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│                        数据层 & 分析引擎                          │
│                                                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ StockAnalyzer   │  │ MainlineEngine  │  │ LeaderMonitor   │ │
│  │ (现有系统)       │  │ (待集成)        │  │ (待集成)        │ │
│  │                 │  │                 │  │                 │ │
│  │ · 技术指标      │  │ · 主线识别      │  │ · 龙头筛选      │ │
│  │ · 财务分析      │  │ · 强度计算      │  │ · 超跌检测      │ │
│  │ · 情绪分析      │  │ · 趋势判断      │  │ · 背离识别      │ │
│  │ · 资金流向      │  │                 │  │                 │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
│          ▲                    ▲                    ▲            │
│          │                    │                    │            │
│          └────────────────────┴────────────────────┘            │
│                               │                                 │
│                               ▼                                 │
│                    ┌──────────────────────┐                     │
│                    │   数据源              │                     │
│                    │  · BaoStock (主)     │                     │
│                    │  · akshare (辅)      │                     │
│                    │  · 本地缓存          │                     │
│                    └──────────────────────┘                     │
└──────────────────────────────────────────────────────────────────┘
```

## 📂 文件结构

```
stock-scanner/2.0 win app/
│
├── 🎨 GUI层
│   ├── gui2.py                          # 主界面（已修改，集成新标签页）
│   ├── quant_mainline_tab.py            # ✅ 量化主线狙击标签页（已创建）
│   └── start.py                         # 启动脚本
│
├── 🧠 核心分析引擎
│   └── stock_analyzer.py                # 现有分析器（技术+财务+情绪）
│
├── ⏳ 量化引擎（待创建）
│   ├── mainline_engine/
│   │   └── mainline_identifier.py      # 主线识别引擎
│   ├── leader_monitor/
│   │   └── leader_selector.py          # 龙头监控器
│   ├── resonance_signal/
│   │   └── signal_engine.py            # 共振信号引擎
│   └── sell_trigger/
│       └── sell_monitor.py             # 卖出触发器
│
├── ⚙️ 配置文件
│   └── config/
│       └── quant_config.json           # 量化系统配置
│
├── 📖 文档
│   ├── 量化主线狙击系统使用指南.md       # ✅ 使用说明（已创建）
│   └── test_quant_integration.py       # ✅ 集成测试脚本（已创建）
│
└── 📊 输出文件
    └── reports/                         # 导出的报告目录
        └── 主线狙击报告_*.md
```

## 🔄 数据流向

```
用户操作
   │
   ▼
┌─────────────────────────────────────┐
│ 1. 点击 "🔍 盘前扫描" 按钮           │
└─────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────┐
│ 2. 创建 QuantMainlineThread 线程   │
│    (避免阻塞GUI)                    │
└─────────────────────────────────────┘
   │
   ├─→ 进度: 10% "🔍 正在识别主线行业..."
   │
   ▼
┌─────────────────────────────────────┐
│ 3. 调用 MainlineEngine              │
│    identify_mainlines()             │
│    └─→ 获取所有行业股票列表         │
│        └─→ 调用 analyzer 批量分析   │
│            └─→ 计算强度分数         │
└─────────────────────────────────────┘
   │
   ├─→ 进度: 40% "✅ 识别到 5 个主线"
   │
   ▼
┌─────────────────────────────────────┐
│ 4. 调用 LeaderMonitor               │
│    build_leader_pool(mainlines)     │
│    └─→ 筛选综合龙头 (综合评分)      │
│    └─→ 筛选资金龙头 (资金流入)      │
└─────────────────────────────────────┘
   │
   ├─→ 进度: 70% "✅ 龙头股池 10 只"
   │
   ▼
┌─────────────────────────────────────┐
│ 5. 调用 LeaderMonitor               │
│    identify_oversold_candidates()   │
│    └─→ 计算超跌系数                 │
│    └─→ 检测技术背离                 │
│    └─→ 判断支撑位置                 │
└─────────────────────────────────────┘
   │
   ├─→ 进度: 100% "✅ 发现 3 个超跌机会"
   │
   ▼
┌─────────────────────────────────────┐
│ 6. 返回结果给GUI                    │
│    finished.emit(results)           │
└─────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────┐
│ 7. 更新三个表格显示                 │
│    - update_mainline_table()        │
│    - update_leader_table()          │
│    - update_oversold_table()        │
└─────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────┐
│ 8. 用户查看结果 / 导出报告          │
└─────────────────────────────────────┘
```

## 🎯 当前状态

### ✅ 已完成部分

1. **GUI集成** (100%)
   - ✅ 在gui2.py中添加第4个标签页
   - ✅ 创建QuantMainlineTab界面类
   - ✅ 三个表格组件（主线/龙头/超跌）
   - ✅ 按钮控制（扫描/监控/导出）
   - ✅ 进度条和状态提示

2. **线程管理** (100%)
   - ✅ QuantMainlineThread异步分析
   - ✅ 进度信号实时反馈
   - ✅ 错误处理机制

3. **数据展示** (100%)
   - ✅ 模拟数据生成（用于界面测试）
   - ✅ 表格自动填充
   - ✅ 颜色标记（涨跌/状态）

4. **报告导出** (100%)
   - ✅ Markdown格式生成
   - ✅ 文件保存功能

### ⏳ 待集成部分

1. **主线识别引擎** (0%)
   - ⏳ 创建mainline_identifier.py
   - ⏳ 实现行业强度计算
   - ⏳ 集成现有analyzer

2. **龙头监控器** (0%)
   - ⏳ 创建leader_selector.py
   - ⏳ 实现龙头筛选算法
   - ⏳ 实现超跌检测算法

3. **共振信号引擎** (0%)
   - ⏳ 创建signal_engine.py
   - ⏳ 实现7条件判断
   - ⏳ 生成买入信号

4. **卖出触发器** (0%)
   - ⏳ 创建sell_monitor.py
   - ⏳ 实现4类卖出监控
   - ⏳ 持仓跟踪

## 🚀 使用方式

### 方式一：启动完整GUI

```bash
cd "/Users/charlieyang/stock/stock-scanner/2.0 win app"
python gui2.py
```

然后点击 **"🎯 量化主线狙击"** 标签页

### 方式二：使用启动脚本

```bash
python start.py
```

### 方式三：测试集成

```bash
python test_quant_integration.py
```

## 💡 下一步工作

如果要实现完整的量化分析功能（不使用模拟数据），需要：

1. **创建量化引擎文件**
   - 参考之前提供的完整设计文档
   - 创建4个模块的Python文件

2. **修改QuantMainlineThread**
   - 将`_mock_mainlines()`替换为真实的`MainlineEngine.identify_mainlines()`
   - 将`_mock_leaders()`替换为真实的`LeaderMonitor.build_leader_pool()`
   - 将`_mock_oversold()`替换为真实的`LeaderMonitor.identify_oversold_candidates()`

3. **创建配置文件**
   - `config/quant_config.json`
   - 设置各种阈值和参数

4. **测试和优化**
   - 使用历史数据回测
   - 调优参数
   - 验证准确性

---

**当前版本可以正常运行，展示完整的用户界面！** ✅
